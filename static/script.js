document.addEventListener("DOMContentLoaded", () => {
  //console.log("Function saveZipcode() called.");
    function saveZipcode() { 
        var zipcode = document.getElementById("zipcode-input").value;

        // choose how to handle the zipcode here.
        
        // log the zipcode to the console.
        console.log("Zipcode entered:", zipcode);
    }
});

